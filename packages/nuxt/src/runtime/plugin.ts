import { defineNuxtPlugin } from '#app'

export default defineNuxtPlugin((_nuxtApp) => {
  // LiftKit Vue theme initialization
  // The theme is set up via the <LkTheme> component using provideTheme()
  // This plugin can be extended to provide global theme defaults via nuxt.config
})
